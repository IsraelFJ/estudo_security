# Comandos no banco de dados MySQL:

```

CREATE DATABASE IF NOT EXISTS db_projeto;

USE db_projeto;

SHOW TABLES;

SELECT * FROM tab_usuario;

UPDATE tab_usuario SET role = 'ROLE_ADMIN' WHERE id = 1;

```
